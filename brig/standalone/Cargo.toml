
[package]
name = "standalone"
version = "0.1.0"
edition = "2021"

[profile.release]
debug = true
#lto = "fat" small perf bump but increases compile time from 2 mins to 6 mins at time of writing

[dependencies]
aarch64_interpreter = { path = "../brig/plugins/guest/devices/arm/aarch64_interpreter" }
clap = { version = "4.5.4", features = ["derive"] }
rustix = { version = "0.38.34", features = ["mm"] }
log = "0.4.21"

# common = { path = "../../aarch64/common" }
# AddWithCarry = { path = "../../aarch64/AddWithCarry" }
# place_slice_signed = { path = "../../aarch64/place_slice_signed" }
