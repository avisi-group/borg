
[package]
name = "standalone"
version = "0.1.0"
edition = "2021"

[profile.release]
debug = true

[dependencies]
arch = { path = "../../brig-aarch64/arch" }
clap = { version = "4.5.4", features = ["derive"] }
rustix = { version = "0.38.32", features = ["mm"] }
